Arduino Potentiometer-Controlled LED Dimmer
This project lets you control the brightness of an LED using a potentiometer (variable resistor). It works by reading an analog input from the potentiometer and using PWM (Pulse Width Modulation) to adjust the LED brightness.

🛠 Components Needed:
Arduino Board (Uno, Mega, etc.)
LED
Resistor (220Ω) (For LED)
Potentiometer (10kΩ recommended)
Jumper Wires
Breadboard (Optional)
🔌 Circuit Connections:
LED:

Anode (Long leg) → Arduino Pin 9 (PWM pin)
Cathode (Short leg) → GND through a 220Ω resistor
Potentiometer:

Left Pin → 5V
Middle Pin → A0 (Analog input)
Right Pin → GND

💻 Arduino Code:
const int potPin = A0;  // Potentiometer connected to A0
const int ledPin = 9;   // LED connected to PWM pin 9

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  int potValue = analogRead(potPin);       // Read potentiometer value (0-1023)
  int brightness = map(potValue, 0, 1023, 0, 255); // Scale to PWM range (0-255)
  
  analogWrite(ledPin, brightness); // Adjust LED brightness
  delay(10); // Small delay for smooth control
}
🚀 Explanation:
analogRead(potPin); → Reads the potentiometer value (0-1023).
map(potValue, 0, 1023, 0, 255); → Converts it to PWM range (0-255).
analogWrite(ledPin, brightness); → Controls LED brightness using PWM.

✅ Enhancements:

Use an OLED display to show the brightness value.
Add a button to switch between different brightness modes.
Control multiple LEDs or a motor with the potentiometer.